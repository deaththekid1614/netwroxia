# ═══════════════════════════════════════════════════════════════════════════════
# NETWROXIA — Stage 4: Offline LLM Copilot
# Requirements for Mistral 7B + ChromaDB RAG
# ═══════════════════════════════════════════════════════════════════════════════

# LLM Runtime — llama.cpp Python bindings (pure C++, zero cloud)
# Compiles from source on first pip install. Takes ~2-3 min on your machine.
llama-cpp-python==0.2.90

# Vector Database — persistent, local SQLite backend
chromadb==0.5.0

# Embeddings — local sentence transformer (22MB, no API calls)
sentence-transformers==2.7.0

# Text chunking for RAG
langchain-text-splitters==0.2.0

# JSON schema validation for structured output
jsonschema==4.22.0
